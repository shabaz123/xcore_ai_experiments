This is an example application for the xcore.ai evaluation board

To build:
  cmake -G "Unix Makefiles" -B build
  xmake -C build

To run on an XK-EVK-XU316 board:
  xrun --io bin/first_example.xe

Expected behaviour on an XK-EVK-XU316 board:
  "First Example is Running" is printed and the LEDs pulse at 100 kHz

To run on the simulator:
  xsim bin/first_example.xe

Expected behaviour on the simulator:
  "First Example is Running" is printed
